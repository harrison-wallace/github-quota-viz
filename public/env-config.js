// Runtime environment variables — injected by start.sh at container startup.
// For local development, set REACT_APP_API_KEY in your environment or .env.local.
// NOTE: start.sh uses sed to replace the empty string value on the line below.
// Do not change the format of that line without also updating start.sh.
window._env_ = {
  REACT_APP_API_KEY: ''
};
